---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: dhcpservers.hostedcluster.densityops.com
spec:
  group: hostedcluster.densityops.com
  names:
    kind: DHCPServer
    listKind: DHCPServerList
    plural: dhcpservers
    shortNames:
    - dhcpserver
    singular: dhcpserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Ready status
      jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - description: Active DHCP leases
      jsonPath: .status.activeLeases
      name: Active Leases
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DHCPServer is the Schema for the dhcpservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DHCPServerSpec defines the desired state of DHCPServer
            properties:
              image:
                default: ghcr.io/cldmnky/hyperdhcp:latest
                description: Image is the container image for the DHCP server
                type: string
              leaseConfig:
                description: LeaseConfig defines the IP address lease configuration
                properties:
                  leaseTime:
                    default: 1h
                    description: LeaseTime is the DHCP lease duration (e.g., "1h",
                      "24h")
                    type: string
                  rangeEnd:
                    description: RangeEnd is the end of the DHCP IP address pool
                    pattern: ^(?:[0-9]{1,3}\.){3}[0-9]{1,3}$
                    type: string
                  rangeStart:
                    description: RangeStart is the beginning of the DHCP IP address
                      pool
                    pattern: ^(?:[0-9]{1,3}\.){3}[0-9]{1,3}$
                    type: string
                required:
                - rangeEnd
                - rangeStart
                type: object
              networkConfig:
                description: NetworkConfig defines the network parameters for the
                  DHCP server
                properties:
                  cidr:
                    description: CIDR is the IP address range that this DHCP server
                      manages
                    pattern: ^(?:[0-9]{1,3}\.){3}[0-9]{1,3}/[0-9]{1,2}$
                    type: string
                  dnsServers:
                    description: DNSServers is a list of DNS servers to advertise
                      to clients
                    items:
                      type: string
                    type: array
                  gateway:
                    description: Gateway is the default gateway IP address
                    pattern: ^(?:[0-9]{1,3}\.){3}[0-9]{1,3}$
                    type: string
                  networkAttachmentName:
                    description: NetworkAttachmentName is the name of the NetworkAttachmentDefinition
                      to attach
                    type: string
                  networkAttachmentNamespace:
                    description: NetworkAttachmentNamespace is the namespace of the
                      NetworkAttachmentDefinition
                    type: string
                  serverIP:
                    description: ServerIP is the static IP address assigned to the
                      DHCP server
                    pattern: ^(?:[0-9]{1,3}\.){3}[0-9]{1,3}$
                    type: string
                required:
                - cidr
                - gateway
                - serverIP
                type: object
              options:
                description: Options defines additional DHCP options to serve
                items:
                  description: DHCPOption defines a DHCP option to serve to clients
                  properties:
                    code:
                      description: Code is the DHCP option code (1-254)
                      maximum: 254
                      minimum: 1
                      type: integer
                    value:
                      description: Value is the value for this DHCP option
                      type: string
                  required:
                  - code
                  - value
                  type: object
                type: array
            required:
            - leaseConfig
            - networkConfig
            type: object
          status:
            description: DHCPServerStatus defines the observed state of DHCPServer
            properties:
              activeLeases:
                description: ActiveLeases is the number of currently active DHCP leases
                format: int32
                type: integer
              conditions:
                description: Conditions represents the latest available observations
                  of the DHCPServer's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed DHCPServer
                format: int64
                type: integer
              totalLeases:
                description: TotalLeases is the total number of available IP addresses
                  in the pool
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
